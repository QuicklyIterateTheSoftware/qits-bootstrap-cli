# qits-cli-bootstrap — copy to .env and edit. Quarkus reads .env from the working directory, so
# every name here is also an ordinary environment variable: what is in the file and what is in the
# environment mean the same thing, and the environment wins.
#
# Every knob below is the one qits-local-up.sh read, under the same name. The defaults are the
# script's defaults; a line you do not need is a line you can delete.

# --- where the sources are ---------------------------------------------------------------------

# OPTIONAL. The wrapper repository (qits-qits, or a worktree of it) whose submodule checkouts this
# bootstrap clones from. It is also where the generated docker-compose.qits.yml and
# .qits-bootstrap.env land.
#
# Unset is the ordinary case: this CLI lives at cli/qits-cli-bootstrap INSIDE the wrapper, so it
# finds it by walking up from the working directory to the first .gitmodules naming the qits
# submodules. Preflight prints which of the two happened. Set this to run from outside the checkout
# entirely.
#
# On a machine with no checkout at all, the run CLONES the wrapper from QITS_ORG_URL into
# <working directory>/qits-qits and carries on from there — a cold start is a supported start.
# A value set here must then name a path inside the working directory, which is the only place a
# run can create one.
#QITS_WRAPPER_DIR=/home/you/code/qits-qits

# Where those clones go. Seed builds write a placeholder client into the tree and may commit a
# pipeline config, so the sources are a copy and never your own checkout.
#QITS_SRC=.qits-bootstrap-src

# Where a repository with no local checkout under QITS_WRAPPER_DIR is cloned from — and where a
# cold start gets the wrapper itself. Read anonymously; no credentials are used.
#QITS_ORG_URL=https://github.com/QuicklyIterateTheSoftware

# --- ports -------------------------------------------------------------------------------------

# THE PORTS BELOW ARE PUBLISHED, NOT DIALLED. This CLI runs as a container on qits-net and reaches
# the platform at its wire aliases; what these numbers decide is what the generated files publish,
# and therefore what a person's browser, a psql and the host's docker daemon can reach.
#
# The host's ONE published port, bound by qits-platform-edge. Every request to it goes edge -> the
# environment's gateway -> the service. qits-gateway publishes no port of its own any more.
#QITS_PORT=8080

# The host port qits-platform-artifacts publishes for the DOCKER DAEMON's pulls and pushes.
# localhost registries are HTTP-allowed by docker without daemon config, which is the whole trick.
# The seed image builds run with --network host, so this is the number they resolve Maven and the
# mirrored base images through — the one consumer that did not move onto qits-net with the CLI.
#QITS_REGISTRY_PORT=8081

# The host port the platform's PostgreSQL publishes, on 127.0.0.1, for a person with a psql. It is
# kept across the deployer's own cutover, so that door survives. This CLI creates the roles and
# databases over the wire alias on 5432 like every other consumer. 5433 rather than 5432 so a
# postgres already installed here is not a bind conflict.
#QITS_PG_PORT=5433

# What qits-platform-dns publishes, ON UDP AND ON TCP. 53 because that is the port a registrar's
# delegation reaches and a nameserver on any other one is a nameserver nobody can find; the service
# binds 8053 inside the container, since below 1024 needs privileges it should not hold.
#
# TCP IS NOT THE OPTIONAL HALF. dnsjava drops a whole RRset that will not fit a UDP answer, so a name
# with several records answers "truncated" and ZERO records, and the client's TCP retry is the only
# way it ever gets one. Resolvers also probe TCP outright.
#
# MOVE IT ON A WORKSTATION THAT ALREADY HOLDS 53, and most do. Measured on the WSL2 host this
# platform is developed on (2026-08-09): systemd-resolved and WSL's own stub hold port 53, docker
# refuses the publish with "failed to bind host port 0.0.0.0:53: address already in use", and
# `compose up` fails as a WHOLE — so the entire seed stack stops, not just the nameserver. Set 5353
# there. A delegation cannot name a port, so anything but 53 is for local testing rather than for the
# public.
#QITS_DNS_PORT=53

# --- the domain ---------------------------------------------------------------------------------

# THE DOMAIN THIS PLATFORM SERVES, and the one knob every public name hangs off. Unset — the default
# — is a complete platform with no public names: qits-platform-dns runs with no zones (its own README
# calls that a legal state) and qits-platform-edge stays on the one plain-HTTP port it always had.
#
# Set, this run does four things:
#   * gives the nameserver its identity, QITS_DNS_NS_NAMES=ns1.<domain> and
#     QITS_DNS_HOSTMASTER=hostmaster.<domain> — both or neither, because the service turns SOA and NS
#     synthesis off unless it holds the pair;
#   * creates the zone row over its API (409 tolerated, so a rerun is quiet);
#   * gives the edge ports 80 and 443 and a loopback 9000, with a Let's Encrypt certificate slot on
#     the qits-edge-letsencrypt volume, seeded with a self-signed placeholder so it can start;
#   * prints the issuance command and the registrar's steps in the closing report.
#
# IT WRITES NO RECORDS AND TOUCHES NO REGISTRAR. Both need this host's PUBLIC address, which a
# container behind a NAT cannot learn — so the A records and the NS/glue delegation are yours.
#
# Lowercase, at least two labels, no trailing dot, no scheme and no path. A bad value stops the run
# before anything is built: the value becomes a zone row in a public nameserver and a certificate
# request to Let's Encrypt, and a rerun with the spelling fixed undoes neither.
#
# `--domain <domain>` on the command line is the same knob for one run.
#QITS_DOMAIN=qits-dev.eu

# --- what to do --------------------------------------------------------------------------------

# 1 = the seed images and the ci-daemon binary exist already; skip to compose and the pushes. The
# recorded digest in .qits-bootstrap.env is then required.
#QITS_SKIP_BUILD=0

# The environment this platform runs, and the ONE deploy ref is its branch, environment/<name>.
# The four platform services (qits-platform-edge, qits-platform-idp, qits-platform-artifacts,
# qits-platform-docs) deploy from that same ref — there is one instance of each and it joins every
# environment's networks. main is the trunk everywhere and deploys nothing.
#
# IT IS ALSO THE PLATFORM ENVIRONMENT — the one tier whose branch deploys the platform plane. The
# bootstrap creates it with that flag, and the deployer ships a platform service from this branch
# and from no other tier's.
#
# THIS NAME IS INSIDE EVERYTHING. An environment service answers to <name>-qits-<app> on the
# network, its deployed container is qits-pd-<name>-qits-<app>-<id8>, and its idp client id is
# <name>-qits-<app>. Changing it after a bootstrap is a new platform, not a rename — and the
# `environment` phase REFUSES rather than renaming, naming what already stands here.
#
# `--platform-env <name>` on the command line is the same knob for one run.
#QITS_ENV_NAME=prod

# --- waiting -----------------------------------------------------------------------------------
# Seconds. Every one of these is shown on screen while it counts down.

# Per application deployment: a cold GraalVM native build plus a health-gated cutover.
#QITS_DEPLOY_TIMEOUT=3600
# Per replayed release run.
#QITS_RELEASE_TIMEOUT=3600
# Per seed service coming up.
#QITS_HEALTH_TIMEOUT=120
# How often the deployment and run polls ask again.
#QITS_POLL_INTERVAL=10

# --- credentials -------------------------------------------------------------------------------

# The git host's push token: what `-o qits.token=<value>` must equal to push a protected default
# branch. NOT a secret — it is the documented escape hatch, and it must be the same across reruns.
#QITS_PUSH_TOKEN=local-dev

# 1 = machine-token enforcement ON for ci, deployments and platform-artifacts. Enforcement that
# only ever runs in production is enforcement nobody has tested, so it is on by default.
#QITS_MACHINE_AUTH=1

# Pin one idp client's secret instead of letting the bootstrap generate it. Without these, the
# first run generates them and records them in .qits-bootstrap.env, and later runs keep what is
# recorded — a rotated secret locks every already-deployed service out.
#
# A CLIENT ID IS A WIRE ALIAS, so three of these four carry the environment name: the ids below
# are what QITS_ENV_NAME=prod produces. Set another environment name and every key moves with it.
#
# There is no qits-deployments entry and there will not be: that service mints nothing, so it holds
# no client and no secret — only an audience its callers are granted. There is no qits-cd entry
# either: every id moved on 2026-08-08, so no key here is one a pre-rename platform would know.
#QITS_IDP_CLIENT_PROD_QITS_CI_SECRET=
#QITS_IDP_CLIENT_QITS_PLATFORM_ARTIFACTS_SECRET=
#QITS_IDP_CLIENT_PROD_QITS_WORKSPACES_SECRET=
#QITS_IDP_CLIENT_PROD_QITS_GATEWAY_SECRET=

# Pin postgres' passwords instead of letting the bootstrap generate them. Same precedence as the
# secrets above — given, then what .qits-bootstrap.env records, then a fresh random — and the same
# reason to leave them alone.
#
# EVERY ONE MUST BE 16-64 HEX CHARACTERS. They are assembled into SQL that cannot be parametrized,
# so nothing else is accepted and the refusal names the key.
#
# The superuser one applies at initdb ONLY: on a data volume that already holds a cluster it is
# ignored, and the value recorded in .qits-bootstrap.env is the only way back in. Setting it here
# after the first boot changes nothing about the server; it changes what this file claims.
#QITS_PG_SUPERUSER_PASSWORD=
#QITS_PG_DEPLOYMENTS_PASSWORD=

# The databases of the CORE SEED SERVICES: qits-ci's own store and its outbox, qits-platform-idp's,
# qits-platform-dns', and qits-containers' own pair. They are here because those containers boot from
# the seed compose file before any deployer exists to provision anything. The nameserver is the
# loudest about it: it refuses to start without a database rather than answer NXDOMAIN for every
# hostname the platform hands out. The orchestrator refuses for the same class of reason — its rows
# are the record of which containers may exist, so an invented store sees every running container as
# claimed by no row.
#
# THE BOOTSTRAP CREATES THESE ROLES AND THEN NEVER TOUCHES THEM AGAIN. From the first pipeline
# deployment onwards qits-platform-deployments' own registry holds these passwords and rotates them
# when it has to, so a value set here reaches only the seed container and only until then. Every
# other service's database — projects, workspaces, events — is provisioned by the deployer alone
# and has no knob here at all.
#QITS_PG_CI_PASSWORD=
#QITS_PG_CI_EVENTSTREAM_PASSWORD=
#QITS_PG_PLATFORM_IDP_PASSWORD=
#QITS_PG_PLATFORM_DNS_PASSWORD=
#QITS_PG_CONTAINERS_PASSWORD=
#QITS_PG_CONTAINERS_EVENTSTREAM_PASSWORD=

# --- the display -------------------------------------------------------------------------------

# 0 = never draw the live display, even on a terminal that could take it. `--no-tui` does the same
# for one run. A pipe or a dumb TERM falls back on its own.
#QITS_TUI=1

# Lines of the running step's output kept in memory for the body region, and for the browser view.
#QITS_TAIL_LINES=2000

# The second column of the lower region: what the PLATFORM announces while the boot runs, read from
# qits-events every few seconds. 0 = do not follow it. The service is one the boot deploys, so it
# answers nothing for the first phases and nothing is said about that either way. A terminal under
# 100 columns keeps one column and drops the pane; a line stream marks the same lines `  ev| `.
#QITS_EVENTS_FEED=1

# The browser view: the same run, live, at http://localhost:8480. It is printed when the run
# starts. 0 = off entirely, and then no port is bound at all.
#QITS_WEB=1

# 8480 because 8080 is the edge and 8081 is the artifacts service. A port already in use STOPS the CLI
# before the boot starts, so a second run at the same time needs this moved, or QITS_WEB=0.
#QITS_WEB_PORT=8480

# Who can reach it. 0.0.0.0 puts the view on whatever network this machine is joined to — which is
# what watching a bootstrap from another machine needs, and a decision to make on purpose.
# 0.0.0.0 by default (WSL2: the Windows-side browser cannot reliably reach a WSL-loopback
# bind). Set 127.0.0.1 to keep the view off the LAN.
#
# The run is a container, so this is the HOST SIDE of the publish now: the server binds every
# interface of a container that has one address, and 127.0.0.1 here still means loopback only.
#QITS_WEB_HOST=0.0.0.0

# The full log of every command, in the working directory.
#QITS_LOG_FILE=qits-bootstrap-cli.log

# --- not yours to set -----------------------------------------------------------------------------

# The same binary runs twice per bootstrap: on the host, where it builds the payload image and
# starts a container, and inside that container, where the phases run. The launcher sets these two
# for the payload and a person sets neither. QITS_IN_CONTAINER=1 on the host runs the phases where
# the platform's addresses do not resolve; QITS_WEB_BIND is QITS_WEB's answer, told to the only half
# that may hold the port.
#QITS_IN_CONTAINER=
#QITS_WEB_BIND=

